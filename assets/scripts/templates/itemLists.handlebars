
<div class="column">
  {{#each groceryLists.items as |item|}}
  {{log items}}
  <div class="container">
  <div class="row row-item" data-item-id="{{_id}}">
    <p>item:&nbsp{{itemName}}&nbsp</p>
    <p>amount:{{amount}}</p>
  </div>
  </div>
  {{/each}}
</div>
